{
  "permissions": {
    "deny": [
      "Bash(*git push*origin main*)",
      "Bash(*git push*origin dev*)",
      "Bash(*git push*--force*)",
      "Bash(*git push*-f *)",
      "Bash(*git merge*)",
      "Bash(*gh pr merge*)",
      "Bash(*rm -rf*)",
      "Bash(*git reset --hard*)"
    ]
  }
}
